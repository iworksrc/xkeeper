import { OverflowInterceptor } from './overflow.interceptor';

describe('OverflowInterceptor', () => {
  it('should be defined', () => {
    expect(new OverflowInterceptor()).toBeDefined();
  });
});
